# Python dependencies for the local repo scripts.
#
# The current scripts (`check-docs-integrity.py`) use only the standard
# library; this file is reserved for future scripts (PR-summary helpers,
# blueprint utilities, etc.) that may need third-party packages. Currently
# empty by design.
